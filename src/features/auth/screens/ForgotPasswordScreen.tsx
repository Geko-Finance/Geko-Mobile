import { ScreenPlaceholder } from "@/src/features/shared/components/ScreenPlaceholder";

export function ForgotPasswordScreen() {
  return (
    <ScreenPlaceholder
      eyebrow="Auth"
      title="Forgot Password"
      description="Credential recovery flow placeholder."
    />
  );
}
