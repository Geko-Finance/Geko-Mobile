import { ScreenPlaceholder } from "@/src/features/shared/components/ScreenPlaceholder";

export function SendPaymentScreen() {
  return (
    <ScreenPlaceholder
      eyebrow="Payments"
      title="Send"
      description="Payment creation route placeholder."
    />
  );
}
