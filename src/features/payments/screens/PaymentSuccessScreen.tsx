import { ScreenPlaceholder } from "@/src/features/shared/components/ScreenPlaceholder";

export function PaymentSuccessScreen() {
  return (
    <ScreenPlaceholder
      eyebrow="Payments"
      title="Success"
      description="Payment completion route placeholder."
    />
  );
}
