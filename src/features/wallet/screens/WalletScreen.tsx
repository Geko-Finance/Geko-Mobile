import { ScreenPlaceholder } from "@/src/features/shared/components/ScreenPlaceholder";

export function WalletScreen() {
  return (
    <ScreenPlaceholder
      eyebrow="App"
      title="Wallet"
      description="Accounts, balances, and wallet entry points."
    />
  );
}
