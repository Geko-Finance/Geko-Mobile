import { ScreenPlaceholder } from "@/src/features/shared/components/ScreenPlaceholder";

export function RegisterScreen() {
  return (
    <ScreenPlaceholder
      eyebrow="Auth"
      title="Register"
      description="New customer registration flow placeholder."
    />
  );
}
