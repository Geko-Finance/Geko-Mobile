import { ScreenPlaceholder } from "@/src/features/shared/components/ScreenPlaceholder";

export function PaymentsScreen() {
  return (
    <ScreenPlaceholder
      eyebrow="App"
      title="Payments"
      description="Payment hub for send, confirm, and success flows."
    />
  );
}
